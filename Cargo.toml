[workspace]
resolver = "2"
members = [
  "core/ipassword-core",
  "core/ipassword-ffi",
  "shared",
  "windows",
  "linux",
]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
overflow-checks = true
strip = true
